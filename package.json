{
  "name": "ffmpeg-renderer",
  "version": "1.0.0",
  "description": "A simple Express server to run FFmpeg rendering jobs.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
